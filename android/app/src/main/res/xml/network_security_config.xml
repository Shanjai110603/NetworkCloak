<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- All domains default to HTTPS (system trust anchors only) -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </base-config>

    <!-- Debug builds allow cleartext for local testing -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system"/>
            <certificates src="user"/>
        </trust-anchors>
    </debug-overrides>
</network-security-config>
